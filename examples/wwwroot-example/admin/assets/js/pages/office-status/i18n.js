"use strict";

(function () {
    const officeStatusMessage = {
        vi: {
            PAGE_TITLE: "Trạng thái phòng công chứng"
        }
    }
    I18n.load("office_status", officeStatusMessage);
})();