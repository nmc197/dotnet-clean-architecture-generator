"use strict";

(function () {
    const userStatusMessage = {
        vi: {
            ACTIVITYLOG: "Lịch sử hoạt động",
            AUDITLOG: "Lịch sử kiểm tra"
        }
    }
    I18n.load("log", userStatusMessage);
})();