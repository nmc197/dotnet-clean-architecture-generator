{{#each usingStatements}}
using {{this}};
{{/each}}

namespace {{projectName}}.Domain.Interfaces.Repositories
{
    public interface I{{name}}Repository : IRepositoryBase<{{name}}, int>
    {
        Task<DTResult<{{name}}Aggregate>> GetPagedAsync({{name}}DTParameters parameters);
    }
}
