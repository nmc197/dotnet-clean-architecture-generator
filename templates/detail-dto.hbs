{{#each usingStatements}}
using {{this}};
{{/each}}

namespace {{projectName}}.Application.DTOs.{{dtoNamespace}}
{
    public class {{name}}DetailDto
    {
        public int Id { get; set; }
        public string Name { get; set; } = null!;
        public string? Description { get; set; }
        public DateTime CreatedDate { get; set; }
        public int? CreatedBy { get; set; }
        public DateTime? LastModifiedDate { get; set; }
        public int? UpdatedBy { get; set; }
{{#each customProperties}}
        public {{csharpType}} {{name}} { get; set; }{{#if defaultValue}} = {{defaultValue}}{{/if}};
{{/each}}
{{#each relationships}}
        {{#if isNavigationProperty}}
        public DataItem<int> {{relatedEntity}} { get; set; } = null!;
        {{/if}}
{{/each}}
    }
}
